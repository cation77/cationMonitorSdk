{
  "compilerOptions": {
    "target": "ES2020", // 兼容 Worker 环境
    "module": "esnext", // 保留 ES 模块语法，交给 Rollup 处理
    "declaration": true, // 生成 .d.ts 类型文件 (由 rollup ESM 构建输出)
    "strict": true, // 开启严格模式，代码更健壮
    "moduleResolution": "node", // 按 Node 方式解析模块
    "lib": ["ESNext", "DOM", "WebWorker"] // 包含 ESNext、DOM 和 WebWorker 类型定义
  },
  "include": ["src/**/*"] // 编译 src 下的所有文件
}
